// export function clear(ctx: OffscreenCanvasRenderingContext2D, color?: string) {
//     ctx.fillStyle = color || '#ffffff';
//     ctx.fillRect(0, 0, ctx.canvas.width, ctx.canvas.height);
// }

// export function paths(ctx: OffscreenCanvasRenderingContext2D, styles: Styles, paths: Paths) {
//     applyStyles(ctx, styles);
//     ctx.beginPath();
//     paths.forEach(path => {
//         path.forEach((point, i) => {
//             if (i === 0) {
//                 ctx.moveTo(point[0], point[1]);
//             } else {
//                 ctx.lineTo(point[0], point[1])
//             }
//         });
//     });
//     ctx.stroke();
// }

export const foo = null;
