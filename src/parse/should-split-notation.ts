import { Rhythm } from "./rhythm-track";

export function shouldSplitNotation() {
    
    
    
    // const isFirstBeat = ((tick - timeSignature.changed) / flow.subdivisions) % timeSignature.beats === 0;
    // return isFirstBeat;
}